# Laboratorios de ciberseguridad

El repositorio cuenta con un entorno de desarrollo y compilación basado en [BunJS](https://bun.sh/), despues de instalarlo puede correr el proyecto con el comando:

```
bun dev
```

Asi podras acceder en la ruta `localhost:5173`
